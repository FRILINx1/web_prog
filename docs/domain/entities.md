# 🏛️ Ключові сутності домену (Entities)

Ці сутності є ядром бізнес-логіки і не містять деталей Flask чи HTTP.

## 1. Сутність: User (Контекст: Автентифікація)

Представляє зареєстрованого користувача системи.

| Поле | Тип | Опис |
| :--- | :--- | :--- |
| `id` | Integer | Унікальний ідентифікатор користувача (Primary Key). |
| `username` | String | Унікальне ім'я користувача (логін). |
| `hashed_password` | Bytes | Хешований пароль (за допомогою bcrypt). |

**Мінімальні методи:**
* `check_password(password)`: Перевіряє наданий пароль проти збереженого хешу.

## 2. Сутність: Task (Контекст: Керування Завданнями)

Представляє одне завдання, що належить користувачеві.

| Поле | Тип | Опис |
| :--- | :--- | :--- |
| `id` | Integer | Унікальний ідентифікатор завдання (Primary Key). |
| `user_id` | Integer | ID користувача, якому належить це завдання (Foreign Key до User.id). |
| `title` | String | Назва/опис завдання. |
| `status` | Enum | Статус завдання (`Active` або `Completed`). |
| `created_at` | Datetime | Дата і час створення. |

**Мінімальні методи:**
* `mark_completed()`: Встановлює статус завдання як `Completed`.
* `update_title(new_title)`: Оновлює назву завдання.

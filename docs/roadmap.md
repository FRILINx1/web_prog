# 📅 MyTaskApp: Roadmap (План розробки на найближчі 4 тижні)

Мета цього етапу — перетворити базовий додаток для входу користувачів на повноцінний менеджер завдань.

## Тиждень 3: Створення моделі завдань та endpoint

| Завдання | Опис |
| :--- | :--- |
| **Модель "Tasks"** | Додати нову таблицю `tasks` до `test.db` (id, user_id, title, is_done). |
| **Endpoint /tasks** | Реалізувати GET-запит на `/tasks` для відображення списку завдань користувача. |
| **Форма створення** | Додати форму у `dashboard.html` для створення нового завдання. |

## Тиждень 4: Функціонал CRUD (Частково)

| Завдання | Опис |
| :--- | :--- |
| **Створення (Create)** | Реалізувати логіку POST-запиту: користувач створює завдання, воно зберігається у БД і прив'язується до `user_id`. |
| **Відображення (Read)** | Оновити дашборд, щоб відображати **реальні** завдання з бази даних. |
| **UX/UI Покращення** | Додати елементарне CSS-оформлення для читабельності списку завдань. |

## Тиждень 5: Завершення CRUD та безпека даних

| Завдання | Опис |
| :--- | :--- |
| **Оновлення (Update)** | Додати логіку для позначення завдання як виконаного (`is_done = True`) через PATCH/PUT. |
| **Видалення (Delete)** | Реалізувати endpoint для видалення завдання (D - Delete). |
| **Контроль доступу** | Гарантувати, що користувач **А** не може оновлювати/видаляти завдання, що належать користувачеві **Б**. | 
